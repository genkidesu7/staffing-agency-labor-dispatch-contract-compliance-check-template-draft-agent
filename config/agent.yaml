# Agent Manifest — AgentRegistry entry point
# AgentRegistry (mediator/registry/agent_registry.py) reads every key below at
# ROOT level — do NOT nest keys under an `agent:` block, per the AgentRegistry
# manifest schema contract.
id: "SVC-C2-047"
name: "Staffing Agency Labor Dispatch Contract Compliance Check & Template Draft Agent"
namespace: "svc"
version: "0.1.0"
enabled: true
category: "Cat 2"
generation_mode: "llm"
industry: "SVC"

# Opaque compatibility metadata. Not an inheritance selector or gate input.
base_type: "RAGAgent"  # RAGAgent | ChatAgent | ReActAgent | DocGenerationAgent

# Entry point — single dotted import path (module.Class), NOT split module:/class:.
class: "src.graph.graph.SvcC2047StaffingContractComplianceAgent"

# Security
required_trust_level: "VERIFIED_EXTERNAL"

# Compile-time gates (AgentRegistry._compile_and_cache) — declare every secret/extra
# actually used, or the agent loads with those checks silently disabled.
requires:
  secrets: ["AZURE_OPENAI_API_KEY", "AZURE_OPENAI_ENDPOINT", "AZURE_OPENAI_DEPLOYMENT", "LABOR_LAW_KB_API_KEY"]
  extras: ["openai"]
